# Setup an ubuntu-desktop install which can logout and login
Tests: setup-test-bed
Depends: @builddeps@
Restrictions: rw-build-tree, isolation-machine, allow-stderr

Tests: small
Depends: @builddeps@
Restrictions: rw-build-tree, isolation-machine, allow-stderr

